version: '3.8'
services:
  pdf:
    image: kooldev/puppeteer:latest
    command: ['yarn', 'start']
    working_dir: '/app'
    user: pptruser
    volumes:
      - '.:/app'
    ports:
      - '3000:3000'
